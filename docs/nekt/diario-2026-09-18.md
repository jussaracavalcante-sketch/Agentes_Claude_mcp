# Diário — 2026-09-18

## 1 · Conferência da madrugada

**As duas Refined materializaram sozinhas**, como o gatilho de evento previa:

| Tabela | Rodou | Resultado |
|---|---|---|
| `rfn_cadastro__cliente` (`query-65kE`) | 07:03 | 407 clientes — 349 por CNPJ, 58 por id iClips |
| `rfn_operacao__peca` (`query-jdUw`) | 07:09 | 134.717 peças, 99.166 etapas de retrabalho, 39,2% das peças com retrabalho |

Os números batem com a construção. Confirma a correção de ontem: elas tinham gatilho, e
correto — faltava o ciclo passar.

**A Unipar ainda não chegou no warehouse, e não chegaria hoje.** A
`trs_google_ads__insight_diario` segue com 36 fontes, zero Unipar, `_extraido_at` de 15/09.
As duas queries disparam por evento na `google-ads-cwt3`, que é **semanal, às terças**.
Próxima execução: **22/09**.

**Erro meu de expectativa.** Ontem escrevi "amanhã de manhã vale conferir se as duas Trusted
rodaram limpas". Não iam rodar — eu mesma documentei no 1-pager que a cadência do Google é
terça. O código está no ar; o dado e a prova em execução real só saem na terça.

---

## 2 · `trs_google_ads__segmento_genero` compactada

Primeira das cinco queries grandes que sobraram. Escolhida por ser a menor, para servir de
prova antes de mexer nas outras quatro.

**De 32.318 para 13.986 caracteres.**

### O que mudou, e por que não é cópia da receita de ontem

As tabelas de breakdown (`gender_performance` e irmãs) **não trazem `account_id` nem
`resource_name`**. O `id_conta` precisa ser injetado — e estava escrito como **literal dentro
de cada um dos 39 blocos**. Isso é pior do que verbosidade: se um literal estivesse errado,
não havia como perceber de uma olhada.

Na forma nova:

| Antes | Depois |
|---|---|
| 39 blocos × 20 colunas nomeadas | colunas nomeadas **uma vez**, na CTE `uniao` |
| `id_conta` literal em 39 pontos | CTE `conta_por_fonte`, **39 pares numa lista** |
| corrigir conta repontada = achar o bloco | corrigir = **uma linha, num lugar só** |

### A sutileza que quase passou

O `_payload_hash` é `MD5(TO_JSON_STRING(x))` sobre a linha **crua**. Se eu calculasse depois
da união, ele passaria a incluir o `_fonte` e **todo hash da tabela mudaria** — a tabela
inteira pareceria nova sem nada ter mudado na origem. O hash ficou dentro de `bruto`, antes
de qualquer coluna acrescentada.

### Equivalência medida antes do deploy

| | |
|---|---|
| Fontes nos dois lados | 36 |
| Linhas · investimento · impressões · cliques | **36 de 36 iguais** |
| `id_conta` por fonte | **36 de 36 iguais** |
| **Hashes só na versão nova** | **0** |
| **Hashes só na versão atual** | **0** |

O conjunto de `(_fonte, _payload_hash)` é idêntico dos dois lados. Isso prova identidade
**linha a linha** da carga crua — teste mais forte que os agregados que usei ontem, e que
passo a usar como padrão nas próximas.

### Estado

Publicada, `deploy_failed: false`, **alerta de falha ligado**. Como as demais de Google Ads,
só executa na terça — a prova em execução real sai em 22/09, junto com a Unipar.

**A Unipar não entrou nesta.** Ficou registrado na descrição: agora são duas linhas por fonte,
uma em `bruto` e uma em `conta_por_fonte`, com o teste de esquema antes.

### Faltam quatro

| Query | Tamanho |
|---|---:|
| `trs_google_ads__termo_busca` | 36.665 |
| `trs_google_ads__segmento_localizacao_usuario` | 36.254 |
| `trs_google_ads__segmento_faixa_etaria` | 32.980 |
| `trs_google_ads__segmento_geografico` | 31.703 |
