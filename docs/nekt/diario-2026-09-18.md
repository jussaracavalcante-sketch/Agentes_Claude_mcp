# Diário — 2026-09-18

## 1 · Conferência da madrugada

**As duas Refined materializaram sozinhas**, como o gatilho de evento previa:

| Tabela | Rodou | Resultado |
|---|---|---|
| `rfn_cadastro__cliente` (`query-65kE`) | 07:03 | 407 clientes — 349 por CNPJ, 58 por id iClips |
| `rfn_operacao__peca` (`query-jdUw`) | 07:09 | 134.717 peças, 99.166 etapas de retrabalho, 39,2% das peças com retrabalho |

Os números batem com a construção. Confirma a correção de ontem: elas tinham gatilho, e
correto — faltava o ciclo passar.

**A Unipar ainda não chegou no warehouse, e não chegaria hoje.** A
`trs_google_ads__insight_diario` segue com 36 fontes, zero Unipar, `_extraido_at` de 15/09.
As duas queries disparam por evento na `google-ads-cwt3`, que é **semanal, às terças**.
Próxima execução: **22/09**.

**Erro meu de expectativa.** Ontem escrevi "amanhã de manhã vale conferir se as duas Trusted
rodaram limpas". Não iam rodar — eu mesma documentei no 1-pager que a cadência do Google é
terça. O código está no ar; o dado e a prova em execução real só saem na terça.

---

## 2 · `trs_google_ads__segmento_genero` compactada

Primeira das cinco queries grandes que sobraram. Escolhida por ser a menor, para servir de
prova antes de mexer nas outras quatro.

**De 32.318 para 13.986 caracteres.**

### O que mudou, e por que não é cópia da receita de ontem

As tabelas de breakdown (`gender_performance` e irmãs) **não trazem `account_id` nem
`resource_name`**. O `id_conta` precisa ser injetado — e estava escrito como **literal dentro
de cada um dos 39 blocos**. Isso é pior do que verbosidade: se um literal estivesse errado,
não havia como perceber de uma olhada.

Na forma nova:

| Antes | Depois |
|---|---|
| 39 blocos × 20 colunas nomeadas | colunas nomeadas **uma vez**, na CTE `uniao` |
| `id_conta` literal em 39 pontos | CTE `conta_por_fonte`, **39 pares numa lista** |
| corrigir conta repontada = achar o bloco | corrigir = **uma linha, num lugar só** |

### A sutileza que quase passou

O `_payload_hash` é `MD5(TO_JSON_STRING(x))` sobre a linha **crua**. Se eu calculasse depois
da união, ele passaria a incluir o `_fonte` e **todo hash da tabela mudaria** — a tabela
inteira pareceria nova sem nada ter mudado na origem. O hash ficou dentro de `bruto`, antes
de qualquer coluna acrescentada.

### Equivalência medida antes do deploy

| | |
|---|---|
| Fontes nos dois lados | 36 |
| Linhas · investimento · impressões · cliques | **36 de 36 iguais** |
| `id_conta` por fonte | **36 de 36 iguais** |
| **Hashes só na versão nova** | **0** |
| **Hashes só na versão atual** | **0** |

O conjunto de `(_fonte, _payload_hash)` é idêntico dos dois lados. Isso prova identidade
**linha a linha** da carga crua — teste mais forte que os agregados que usei ontem, e que
passo a usar como padrão nas próximas.

### Estado

Publicada, `deploy_failed: false`, **alerta de falha ligado**. Como as demais de Google Ads,
só executa na terça — a prova em execução real sai em 22/09, junto com a Unipar.

**A Unipar não entrou nesta.** Ficou registrado na descrição: agora são duas linhas por fonte,
uma em `bruto` e uma em `conta_por_fonte`, com o teste de esquema antes.

### Faltam quatro

| Query | Tamanho |
|---|---:|
| `trs_google_ads__termo_busca` | 36.665 |
| `trs_google_ads__segmento_localizacao_usuario` | 36.254 |
| `trs_google_ads__segmento_faixa_etaria` | 32.980 |
| `trs_google_ads__segmento_geografico` | 31.703 |

## 3 · As quatro restantes compactadas

Mesma receita da `segmento_genero`, aplicada às quatro que faltavam. As cinco de breakdown
tinham a mesma forma: uma CTE `uniao` com 39 ramos, cada um nomeando todas as colunas e
carregando o `id_conta` como literal. Agora cada ramo é uma linha em `bruto`
(`SELECT '<slug>', TO_HEX(MD5(TO_JSON_STRING(x))), x.* FROM ...`), os 39 pares
slug→`customer_id` vivem numa CTE `conta_por_fonte` só, e as colunas são nomeadas **uma vez**
na `uniao`. A cauda de cada query ficou byte a byte igual.

| Query | Slug | Antes | Depois |
|---|---|---:|---:|
| `trs_google_ads__segmento_faixa_etaria` | `query-HAB1` | 32.980 | **14.193** |
| `trs_google_ads__segmento_geografico` | `query-tmws` | 31.703 | **14.057** |
| `trs_google_ads__segmento_localizacao_usuario` | `query-pYmL` | 36.254 | **14.317** |
| `trs_google_ads__termo_busca` | `query-vdre` | 36.665 | **14.422** |

Com a `segmento_genero` (32.318 → 13.986) e as duas de ontem (`query-tL4g` 76.400 → 25.972,
`query-zF8L` 45.615 → 15.426), as **sete** queries de Google Ads estão na forma compacta.
Nenhuma delas passa de 26 mil caracteres; a maior antes tinha 76 mil e era, na prática,
não editável por `update_transformation`.

### Equivalência medida antes de cada deploy

As quatro deram exatamente o mesmo resultado:

| | |
|---|---|
| Fontes nos dois lados | 36 |
| Linhas · investimento · impressões · cliques | **36 de 36 iguais** |
| `id_conta` por fonte | **36 de 36 iguais** |
| **Hashes só na versão nova** | **0** |
| **Hashes só na versão atual** | **0** |

`EXCEPT DISTINCT` nas duas direções sobre `(_fonte, _payload_hash)` — identidade linha a
linha, não só agregado. E como a união com `*` falha no plano quando um esquema diverge, a
própria validação serviu de teste de esquema nas 36 fontes.

### Estado

As quatro publicadas, `deploy_failed: false`, **alerta de falha ligado** nas quatro.
Descrições reescritas: o texto antigo afirmava que o `id_conta` era literal por ramo, o que
deixou de ser verdade; entrou a explicação do `conta_por_fonte`, o custo do `SELECT *`
(divergência de esquema numa conta quebra a união inteira, não só aquele ramo), a receita do
`SELECT f FROM (<união>) LIMIT 0` antes de publicar, o motivo de o `_payload_hash` ser
calculado dentro do bloco cru, e os números da equivalência com data.

### O que ainda não entrou

**A Unipar não está em nenhuma das cinco de breakdown** — por decisão, some depois. Com a
forma compacta são agora **duas linhas por fonte** em cada query (uma em `bruto`, uma em
`conta_por_fonte`) em vez de um bloco de 26 colunas.

E a ressalva de sempre: nenhuma dessas sete executa antes de **terça, 22/09** — todas
disparam por evento da `google-ads-cwt3`, que roda às terças. Deploy limpo não é execução
limpa; a prova sai lá.

## 4 · Unipar nas cinco de breakdown

Com a forma compacta pronta, somar as 3 fontes do Grupo Unipar virou duas linhas por
query: uma em `bruto` e uma em `conta_por_fonte`. Na forma antiga teria sido um bloco de
20 a 26 colunas repetido três vezes em cada uma das cinco — o trabalho que manteve a
Unipar fora por meses.

| Fonte | Conta | `customer_id` | Camada |
|---|---|---|---|
| `google-ads-3eFc` | UNIPAR RESIDENCIAL BOA VISTA | 3083428472 | `unipar_boa_vista` |
| `google-ads-mvUx` | UNIPAR NEO VILA ADRIANOPOLIS | 6451568997 | `unipar_neo_vila` |
| `google-ads-hBlk` | UNIPAR PARQUE DAS TORRES | 1911984217 | `unipar_torres` |

### Conferido antes de somar

**Materialização das 15 tabelas** (3 fontes × 5 streams) — todas existem e todas têm dado.
Tabela no catálogo não é tabela existente, e aqui a checagem valeu: se uma faltasse, a união
inteira cairia, não só aquele ramo.

| Stream | Linhas | Investimento | Impressões | Cliques |
|---|---:|---:|---:|---:|
| `geographic` | 4.250 | R$ 95.097,20 | 674.534 | 59.835 |
| `user_location` | 28.724 | R$ 87.832,64 | 626.931 | 53.908 |
| `age_range` | 10.240 | R$ 82.138,11 | 374.491 | 34.956 |
| `gender` | 5.464 | R$ 82.138,11 | 374.491 | 34.956 |
| `search_term` | 89.952 | R$ 37.227,97 | 175.608 | 15.583 |

**Os R$ 95.097,20 do `geographic` são exatamente o total que a Unipar já soma na
`trs_google_ads__insight_diario`** — a tabela que reconcilia continua reconciliando com as
contas novas. As outras quatro cobrem menos, e cada uma pela razão que já estava declarada
na descrição: PMax não publica breakdown demográfico (gênero e faixa etária, 86,4%),
localização física nem sempre resolve (92,4%), e termo de busca só existe em campanha de
rede de pesquisa, com limiar de privacidade por cima (39,1%). `gender` e `age_range` dando
o mesmo total confirma o mecanismo comum de supressão.

**Esquema das 3 contra a referência**, nos cinco streams, com união sob `WHERE FALSE` — falha
no plano se divergir, custo zero de leitura. Passou nos cinco. É o `SELECT f FROM (<união>)
LIMIT 0` que a própria descrição manda rodar ao somar fonte nova; foi a primeira vez que a
receita foi usada de verdade.

### Publicado

As cinco com `deploy_failed: false` e `status: idle`. Descrições reescritas: o parágrafo
"AS 3 FONTES DA UNIPAR AINDA NÃO ESTÃO AQUI" saiu, entrou o registro da entrada com os
números medidos e a ressalva de que os percentuais de cobertura de 08/09 são anteriores às
3 contas e precisam ser refeitos. Os cabeçalhos dos arquivos passaram de 39 para 42 fontes,
e o texto que ainda dizia "id_conta injetado por ramo" — falso desde a compactação — foi
corrigido para apontar a `conta_por_fonte`.

### Onde a Unipar está agora

| Tabela | Unipar? |
|---|---|
| `trs_google_ads__conta` | ✅ 17/09 |
| `trs_google_ads__insight_diario` | ✅ 17/09 — 3.019 linhas, R$ 95.097,20 |
| `trs_google_ads__campanha` | ✅ 17/09 — 9 campanhas |
| `trs_google_ads__segmento_genero` | ✅ 18/09 |
| `trs_google_ads__segmento_faixa_etaria` | ✅ 18/09 |
| `trs_google_ads__segmento_geografico` | ✅ 18/09 |
| `trs_google_ads__segmento_localizacao_usuario` | ✅ 18/09 |
| `trs_google_ads__termo_busca` | ✅ 18/09 |

**As sete queries de Google Ads estão na forma compacta e as oito tabelas enumeram as 42
fontes.** Nenhuma delas executa antes de **terça, 22/09** — todas disparam por evento da
`google-ads-cwt3`. Código publicado não é dado materializado; a prova sai lá.

## 5 · PIs da Fogás, competência setembro

Consulta pedida no meio do dia. Resposta e a ressalva que importa mais que ela:

`vanguardamartech_raw.supabase_silver_pi_insercao` traz **8 PIs para SOCIEDADE FOGÁS em
2026-09**, R$ 120.802,00 negociados, todos do projeto 29082 "FOGAS | CAMPANHAS ANUAIS 2026",
todos TV (TV Amazonas/Globo em Manaus, Porto Velho e Rio Branco; TV Roraima em Boa Vista),
responsável Gilmar de Souza Miranda, todos "Aguardando Faturamento".

**Esse número não é o mês.** Setembro, outubro, novembro e dezembro trazem os mesmos 8 PIs e
o mesmíssimo valor — é o contrato anual pré-lançado. A Fogás roda 30 a 35 PIs/mês
historicamente (jul/26: 33, R$ 296.196,01; jun/26: 34, R$ 307.207,08); agosto já aparece
truncado em 20.

**Causa: a fonte está parada.** Maior PI na base = 22930; última `data_aprovacao_proposta` =
06/08/2026. A competência setembro inteira tem 34 linhas e 14 clientes — não é problema da
Fogás. O `silver_pi_insercao` é carregado por worker do lado do Supabase, a mesma pendência
de terceiro que já estava na lista junto com `bronze__projetos` (congelada em 24/08).
Para o número real de setembro hoje, só o iClips direto.
